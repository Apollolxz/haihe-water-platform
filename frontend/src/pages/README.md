# Native React Pages

Move pages here one at a time as they are migrated away from `src/generated/LegacyPages.jsx`.

Keep visual parity with the legacy page before removing the matching `frontend/pages/*.html` source and `assets/js/*` runtime script.
